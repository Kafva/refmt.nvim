#include <stdio.h>

int main(int argc, char *argv[], char *envp[]) {
    int r = snprintf(args, sizeof(args), "video_size=%dx%d:pix_fmt=%d:time_base=%d/%d:pixel_aspect=%d/%d", ctx->dec_codec_ctx->width,ctx->dec_codec_ctx->height, ctx->dec_codec_ctx->pix_fmt, VIDEO_INPUT_STREAM(ctx)->time_base.num, VIDEO_INPUT_STREAM(ctx, time(NULL), 2)->time_base.den, ctx->dec_codec_ctx->sample_aspect_ratio.num, ctx->dec_codec_ctx->sample_aspect_ratio.den);
    return r;
}

